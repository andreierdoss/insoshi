%h2
  = t 'comment.wall_comment'
= error_messages_for :comment
%h3
  = t 'comment.leave_a_comment'
- form_for :comment, :url => person_comments_path(@person) do |f|
  .form_row
    = f.text_area :body
  .form_row
    = submit_tag t('global.submit'), :class => "button"
